<!doctype html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>Buys</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="./home.css">
</head>
<body>
      <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <ul class="nav navbar-nav">
                    <li role="presentation"><a href="/">Home</a></li>

                    <li role="presentation"><a href="/pets">Pets</a></li>
                    <li role="presentation"><a href="/adopters">Adopters</a></li>
                    <li role="presentation" class="active"><a href="/accessories">Accessories</a></li>
		    <li role="presentation"><a href="/species">Species</a></li>
                </ul>
            </div>
        </div>
    </nav>


    <div class="container">
	<div class="jumbotron">
	    <h1>Add an Accessory for an Adopter</h1>
        </div>

	<h3>Choose Adopter and Accessory</h3>
	<form id="add_buys" action = "/buys" method="post">
	   <div class="form-group">
		<label for="adopters">Adopter:</label>
		<select class="form-control" name="adopters" id="adopters">
			{{#each adopters}}
				<option value="{{id}}">{{fName}}</option>
			{{/each}}
		</select>
	    </div>
	    <div class="form-group">
		<label for="accessories">Accessory Adopter Bought: </label>
		<select class="form-control" name="accessories" id="accessories">
			{{#each accessories}}
				<option value="{{id}}">{{name}}</option>
			{{/each}}
		</select>
	    </div>
	    <button type="submit" class="btn btn-primary">Submit</button>
	</form>
	<br/>
	<h3>List of Accessories Bought:</h3>
	<table class="table table-striped">
		<thead>
		    <tr>
			<th scope="col">Adopter</th>
			<th scope="col">Accessory</th>
		    <tr>
		</thead>
		<tbody>
			{{#each buys}}
			<tr>
				<td>{{adopterID}}</td>
				<td>{{accessoryID}}</td>
			</tr>
			{{/each}}
		</tbody>
	</table>
    </div>
</body>
</html>
